module.exports = {
	up: function(db) {
		// db.createTable(tableName, attributes, options);
		// db.dropTable(tableName);
		// db.renameTable(before, after);
		// db.addColumn(tableName, attributeName, dataTypeOrOptions);
		// db.removeColumn(tableName, attributeName);
		// db.renameColumn(tableName, attrNameBefore, attrNameAfter);
		// db.addIndex(tableName, attributes, options);
		// db.removeIndex(tableName, indexNameOrAttributes);
	},

	down: function(db) {

	}
}